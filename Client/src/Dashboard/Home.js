import React from 'react';
// reuseable component 
import Divider from '../resuableComponents/Divider'



const Home = (props) => {
    return (
        <div>
            <Divider  text={'Dashboard'}/>
        </div>
    );
};

export default Home;